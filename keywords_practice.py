# Python have around 36 keywords, this keywords cannot be used as
# a variable names, file names, class names, functions etc

"""
False, True, None
and, or, not, in, is
if, elif, else
for, while, break, continue, pass
def, class, with, as , lambda
return, yield
import, from
try, except, raise, finally, assert
async, await
del, global, nonlocal

"""

a=10
del a
print(a)